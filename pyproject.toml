[project]
name = "demo-py-portfoliorisk"
version = "0.1.0"
description = "A data-pipeline that uses Yahoo Finance and Numpy to collect live price data and calculate a rolling Value at Risk (VaR)"
readme = "README.md"
requires-python = ">=3.14.5"
dependencies = [
    "numpy>=2.4.6",
    "yfinance>=1.4.1",
]
